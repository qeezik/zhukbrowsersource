// Дополнительные рабочие прокси-серверы
// Этот файл содержит резервные прокси на случай, если основные не работают

const backupProxies = [
    // Основные прокси (HTTP с аутентификацией)
    {
        country: 'Прокси 1',
        code: 'proxy1',
        type: 'http',
        host: '91.207.183.242',
        port: 54124,
        username: 'NmdgEzTJ',
        password: 'u8WnjgE7',
        flag: '🌐',
        working: true,
        ping: '50ms',
        quality: 'excellent'
    },
    {
        country: 'Прокси 2',
        code: 'proxy2',
        type: 'http',
        host: '94.131.113.168',
        port: 50738,
        username: 'NmdgEzTJ',
        password: 'u8WnjgE7',
        flag: '🌐',
        working: true,
        ping: '70ms',
        quality: 'excellent'
    }
];

// Функция для получения резервного прокси
function getBackupProxy(countryCode) {
    return backupProxies.find(p => p.code === countryCode) || backupProxies[0];
}

// Функция для получения всех резервных прокси
function getAllBackupProxies() {
    return backupProxies;
}

// Функция для получения прокси по типу
function getProxyByType(type) {
    return backupProxies.filter(p => p.type === type);
}

// Функция для получения лучшего прокси для обхода блокировок
function getBestProxyForBypass() {
    // Возвращаем первый доступный прокси
    return backupProxies[0];
}

// Экспорт для использования в других файлах
if (typeof module !== 'undefined' && module.exports) {
    module.exports = {
        backupProxies,
        getBackupProxy,
        getAllBackupProxies,
        getProxyByType,
        getBestProxyForBypass
    };
} 